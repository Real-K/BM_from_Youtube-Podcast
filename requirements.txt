yt-dlp>=2025.1.1
feedparser>=6.0
# insane-search engine (.claude/skills/insane-search) — fetch chain
curl_cffi>=0.7
pyyaml>=6.0
markdownify>=0.13
# optional: PDF extraction / main-content extraction / browser lanes
pypdf>=4.0
# pdfplumber
# resiliparse
# nodriver
# patchright
# 음성 전사(ASR) — 자막도 발행자 전사본도 없는 오디오용
pywhispercpp>=1.2
imageio-ffmpeg>=0.5
# faster-whisper는 Python 3.14 + ctranslate2 4.8에서 세그폴트로 죽는다(2026-09 확인).
# Python 3.12 이하에서만 대안으로 쓴다.
# faster-whisper
